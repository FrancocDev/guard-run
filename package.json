{
  "name": "guard-run",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "guard-run": "./dist/cli.js"
  },
  "scripts": {
    "build": "bun build src/cli.ts --outdir dist",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@francocdev/ts-patterns": "^0.1.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.8.2"
  }
}
